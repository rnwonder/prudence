import React from 'react'
import "./styles.scss"

export interface ITemplate {
    
}

const Template: React.FunctionComponent<ITemplate> = ({}) => {
    return (
        <section className="Template">
            
      </section>
    )
}

export default Template